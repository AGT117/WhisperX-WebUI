# WhisperX WebUI 环境变量配置模板
# 复制本文件为 .env 并填入实际值

# Hugging Face Token (说话人聚类功能必需)
# 获取地址: https://huggingface.co/settings/tokens
HF_TOKEN=API_KEY_HERE

# 运行设备 (可选, 默认自动检测: cuda / cpu)
# DEVICE=cuda

# 计算精度 (可选, GPU 默认 float16, CPU 默认 float32)
# 可选值: float16, int8, float32
# COMPUTE_TYPE=float16

# 批处理大小 (可选, 默认 4, 显存不足时调低)
# BATCH_SIZE=4

# ---- LLM 配置 ----
# LLM API 请在 config/llm_config.json 中配置，不在此文件设置。